[build]
  publish = "dist"
  command = "npm run build"

# ビルドをスキップする条件を指定
# マークダウンファイル (*.md) 以外の変更がない場合はビルドをスキップ (終了コード0でスキップ)
ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- . ':!*.md'"
